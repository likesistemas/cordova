version: '3'

services:
    cordova:
        build: .
        command: dev
        image: likesistemas/cordova:latest
        volumes:
            - ./apk/:/root/apk/
            - ./www/:/root/www/
            - ./.env-app:/root/app/.env-app